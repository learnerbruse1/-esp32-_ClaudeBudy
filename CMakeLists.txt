# ================================================================
# 学而思小喵掌机 — Claude Buddy 固件 (Claude 伙伴 + 投屏 + 工具箱 + 设置)
# ESP32 (WROVER, Quad PSRAM, 4MB Flash) / ESP-IDF v5.4 / LVGL v9
# ================================================================
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(xiaomiao_buddy)
